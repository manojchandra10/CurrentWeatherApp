name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping weather app
        run: curl -fsS --max-time 60 https://current-weather-hl4m.onrender.com/ > /dev/null
